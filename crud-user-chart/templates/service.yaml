apiVersion: v1
kind: Service
metadata:
  name: {{ include "crud-user-chart.fullname" . }}
  labels:
    {{- include "crud-user-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: web
      protocol: TCP
      name: http
  selector:
    {{- include "crud-user-chart.selectorLabels" . | nindent 4 }}
